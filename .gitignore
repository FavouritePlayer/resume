# LaTeX aux/build output (redirected here by latexmk -aux-directory=.build)
.build/

# Stray LaTeX build artifacts (in case of a plain pdflatex run without -aux-directory)
*.aux
*.fdb_latexmk
*.fls
*.log
*.synctex.gz
*.out
*.toc
*.bbl
*.blg

# Never-sent reservoir doc — no need to track its rendered PDF
cv_master.pdf

# macOS
.DS_Store
